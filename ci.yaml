steps:
  # Docker Build
  - name: 'gcr.io/cloud-builders/docker'
    args: [
            'build',
            '-t',
            'asia-southeast1-docker.pkg.dev/gcp-mlops-images/my-image-app:${SHORT_SHA}',
            '-t',
            'asia-southeast1-docker.pkg.dev/gcp-mlops-images/my-image-app:latest',
            '--cache-from',
            'asia-southeast1-docker.pkg.dev/gcp-mlops-images/my-image-app:latest',
            '.'
          ]
  # Docker push to Google Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push',  'asia-southeast1-docker.pkg.dev/gcp-mlops-images/my-image-app:${SHORT_SHA}']

# Store images in Google Artifact Registry 
images:
  - asia-southeast1-docker.pkg.dev/gcp-mlops-images/my-image-app:${SHORT_SHA}
  - asia-southeast1-docker.pkg.dev/gcp-mlops-images/my-image-app:latest

options:
  logging: CLOUD_LOGGING_ONLY